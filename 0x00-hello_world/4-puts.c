#include <stdio.h>
#include <string.h>

/**
 * Main entry point
 *
 * Always returns 0
 *
 */
int main(void)
{
	char str[] = "Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
