#include "main.h"

/**
 * _isupper - this program check for upper case letter
 * @c : character to check the case
 * Return:0 if false or 1 if case is upper
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
