#include "main.h"

/**
 * _isdigit - this program check for upper case letter
 * @c : character to check
 * Return:0 if false or 1 if argument is digit
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
